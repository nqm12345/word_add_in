node_modules/

# Keep Electron version local only (backup)
word-setup-tool/
server/documents/
*.log
.env
.DS_Store
Thumbs.db

# SSL certificates (must be generated on each machine)
certs/*.crt
certs/*.key
certs/*.pem
